# core.cast_memberships - Table Structure Diagram

Auto-generated schema diagram showing columns and data types.
```mermaid
erDiagram
    CORE_CAST_MEMBERSHIPS {
        UUID id
        UUID show_id
        UUID season_id
        UUID person_id
        TEXT role
        INTEGER billing_order
        TEXT notes
        TIMESTAMP_WITH_TIME_ZONE created_at
    }
```
