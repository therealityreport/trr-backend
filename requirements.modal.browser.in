# Browser-capable Modal runtime for social scrapers, SocialBlade, auth probes,
# and Getty probes.
-r requirements.modal.lean.in

crawlee>=1.4.0
scrapling[fetchers]>=0.4.9,<0.5
playwright>=1.60.0
yt-dlp
