{
  "commands": [
    "set -e",
    "systemctl show trr-api -p ExecStart -p EnvironmentFile",
    "command -v python3 || true",
    "ls -la /opt || true",
    "ls -la /srv || true",
    "find / -maxdepth 4 -type f -path '*/bin/python*' 2>/dev/null | head -n 40"
  ]
}
