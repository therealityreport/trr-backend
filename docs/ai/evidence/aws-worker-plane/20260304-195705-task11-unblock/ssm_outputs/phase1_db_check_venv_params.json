{
  "commands": [
    "set -e",
    "DB_URL=$(grep -E '^SUPABASE_DB_URL=|^DATABASE_URL=' /etc/trr-api.env | head -n1 | cut -d= -f2-)",
    "echo DB_URL_PREFIX=${DB_URL%%:*}",
    "DB_URL=\"$DB_URL\" /opt/trr-backend/.venv/bin/python -c \"import os,psycopg; conn=psycopg.connect(os.environ['DB_URL'],connect_timeout=10); cur=conn.cursor(); cur.execute('select 1 from core.admin_operations limit 1;'); row=cur.fetchone(); print(f'db_check_row={row[0] if row else None}'); conn.close()\""
  ]
}
