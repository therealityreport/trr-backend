{
  "commands": [
    "set -e",
    "DB_URL=$(grep -E '^SUPABASE_DB_URL=|^DATABASE_URL=' /etc/trr-api.env | head -n1 | cut -d= -f2-)",
    "DB_URL=\"$DB_URL\" /opt/trr-backend/.venv/bin/python -c \"import os,psycopg2; c=psycopg2.connect(os.environ['DB_URL'],connect_timeout=10); cur=c.cursor(); cur.execute(\\\"select (to_regclass('core.admin_operations') is not null)::int, (to_regclass('core.admin_operation_events') is not null)::int\\\"); print(cur.fetchone()); c.close()\""
  ]
}
