# Branching Strategy

This repository does not prescribe a branching/worktree strategy.

- Default is work on `main`.
- Only create/use a branch or worktree if explicitly asked.
- No branch naming conventions are required by this repo.

