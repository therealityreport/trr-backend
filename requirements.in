# Core pipeline deps
typer>=0.12.0
requests>=2.32.0
curl_cffi>=0.13.0
crawlee>=1.4.0
scrapling[fetchers]>=0.4.9,<0.5
python-dotenv>=1.0.0
gspread>=6.0.0
google-auth>=2.30.0
google-auth-oauthlib>=1.2.0
beautifulsoup4>=4.12.0
tenacity>=8.2.0
numpy==1.26.4

# Gemini integration (used by multiple pipeline stages)
google-genai>=1.0.0

# Firebase deployment helpers (optional; used by upload scripts)
firebase-admin>=6.5.0

# Postgres/core schema helpers (used by sync + schema docs)
psycopg2-binary>=2.9.9
PyJWT>=2.8,<3

# Media mirroring
boto3>=1.35.0
Pillow>=10.0.0
cairosvg>=2.7.1
reportlab>=4.2.0

# API server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
gunicorn>=21.0.0
modal>=1.3.0
redis>=5.2.0
python-multipart>=0.0.20
pydantic>=2.10.0
supabase==2.28.3
postgrest==2.28.3
sse-starlette>=2.0.0

# Claude computer use
anthropic>=0.43.0

# Dev / test
pytest>=8.0.0
httpx>=0.28.0
playwright>=1.60.0

# Local Chrome cookie extraction for operator-run auth repair helpers.
pycookiecheat>=0.8.0,<0.9

# Repository map generation
tree-sitter>=0.21.0
tree-sitter-python>=0.21.0

# Observability (Sentry — env-gated; no-op without SENTRY_DSN)
sentry-sdk[fastapi]>=2.0.0

# Modal ops MCP server (local stdio server powering the modal-ops MCP / Codex modal plugin)
mcp>=1.2.0
